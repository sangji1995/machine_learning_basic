import matplotlib.pyplot as plt

plt.imshow(train_images[0], cmap=plt.cm.binary)
plt.show()

#행렬의 원소별 연산
w=np.array([[1,2,3],
            [4,5,6]])
x=np.array([[0,1,2],
            [3,4,5]])

print(w+x)  

print(w*x) #곱은 위아래로 연산한다 

#브로드 캐스트 연산
a=np.array([[1,2],
            [3,4]])

print(a*10)

b=np.array([10,20]) #횡 벡터로 본다 하지만 계산시 밑에 한곳을 더 넣어서 한다.
#a.T(트랜스포즈)*b
#2차원 행렬 *벡터 계산인 경우
